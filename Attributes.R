#Attributes 
#R objects have attributes(metadata for object)
#Examples of R object attributes(names, dimnames, dimensions[matrices,arrays]
#,class[Integer,numeric],length, other user defined attributes and meta data)
x<-1
x
#not all objects contain attributes, in which case the attributes function returns NULL
attributes(x)
